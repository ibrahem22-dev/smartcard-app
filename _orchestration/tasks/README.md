# tasks/

Executor task packets land here — one file per delegated task, written by the supervisor
before handoff (Tier 2: pasted into a GUI by the Owner; Tier 1: passed as the `agent()`/subprocess
prompt). See `EXECUTOR_CAPABILITIES.md` for the current operating tier and its reasoning.

Empty as of 2026-08-19 — no executor delegation has occurred yet (explicitly out of scope for the
P-1/P0-A/E0/T4 session that created this directory).
