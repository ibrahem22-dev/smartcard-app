# TASK E1 — Architecture-boundary lint rules, diagnostic only
PHASE: E1   LANE: E   EXECUTOR: Cursor (cursor-agent, model claude-opus-5-high)   WORKTREE: C:\Users\ebrah\SmartCard-Agent\app\wt-E1   BRANCH: task/E1-boundary-lint

## OBJECTIVE
Build five custom ESLint rules encoding the SmartCard architecture boundaries, wire them into a
**separate, standalone** ESLint config that is NOT part of the app's main lint gate, and run that
config against the existing inherited codebase to produce a violation report. This is a diagnostic
— the violations it finds are a backlog for later cleanup (P2/P3), not something you fix now.

When you are done, the five rules must exist, must correctly flag every violation named below when
you point them at this codebase, must NOT flag anything in genuinely clean files, and the app's
EXISTING main lint gate (`npx eslint src --ext .ts,.tsx,.js --max-warnings=0`) must still pass
exactly as it does today — you are adding a new, separate check, not modifying the existing one.

## CONTEXT YOU NEED (read these, nothing else)
- `src/engines/cardRoleEngine.ts` (line 19, `getEffectiveFxCommission`) — one of three FX
  implementations that must not coexist silently.
- `src/hooks/useFxAbroad.ts` and `src/engines/fxAbroadEngine.ts` (`resolveFxAbroad`) — the second
  FX implementation. `useFxAbroad.ts` also directly imports a JSON dataset — read it for the
  import pattern R3 must catch.
- `src/hooks/usePurchaseGate.ts` — contains a third, inline FX comparison.
- `src/screens/DecisionScreen.tsx` lines ~100–140 (confirmed: literal `84/100` at line 116, literal
  `71/100` at line 128) — hardcoded scores where an engine result belongs.
- `src/engines/installmentGate.ts`, `src/engines/loanEngine.ts`, `src/engines/purchaseGateRules.ts`
  — three of the four load-threshold ladders (0.25/0.35/0.50-style numeric literals). The fourth is
  the product spec's own 35/50 threshold, which is not code and you cannot flag it — do not go
  looking for a fourth file.
- `src/hooks/useCardRatesDatabase.ts` — the other direct-JSON-import site R3 must catch.
- `.eslintrc.js` (or equivalent) at the repo root — read it to see the existing lint setup and
  config format so your new config is structurally consistent with it (same parser options, same
  plugin conventions), even though it is registered separately.
Do not read the whole repository. Everything you need is listed here.

## SCOPE — files you may create or modify
- `.eslintrc.boundaries.js` (new file, repo root) — the five rules and their config.
- `_orchestration/reports/E1-boundary-violations.md` (new file) — your violation report.
- Nothing else. In particular: do not modify `.eslintrc.js`, `package.json`, `tsconfig.json`, or
  any file under `src/`.

## FORBIDDEN ZONES — do not touch, do not read for inspiration
- `src/**` — READ-ONLY for this task. You are diagnosing it, not fixing it.
- The canonical data estate (`All application data\smartcard-canonical-v1\`) and the donor archive
  (`All application data\_app_archive\`) — you have no reason to go near either, and both are now
  filesystem read-only regardless.
- Never modify: signing keys, `.env*`, lockfiles, CI config, any file outside SCOPE above.
- Do not touch the main tree at `C:\Users\ebrah\SmartCard-Agent\app\SmartCard` — you are in
  `wt-E1`, a separate worktree. Stay in it.

## ARCHITECTURAL CONSTRAINTS (violating any of these fails the task regardless of tests)
Build exactly these five rules, in `.eslintrc.boundaries.js`:

1. **R1** — `engines/**` may not import from `ui/**`, `screens/**`, `components/**`, or any
   network module.
2. **R2** — `ui/**`, `screens/**`, `components/**` may not import any math/calculation utility,
   and may not import from `engines/**/internal/**` — only the engines' public result types.
3. **R3** — nothing outside `data/adapter/**` (or, in this inherited codebase, outside
   `src/authority/**`, which is the nearest existing equivalent — use your judgment and say which
   you chose and why in your report) may import a pack file, a raw JSON dataset, or a local DB
   driver directly.
4. **R4** — no numeric literal that looks like a rate, fee, percentage or threshold outside
   `config/**` and the packs. Allowlist genuine exceptions explicitly, each with a comment
   explaining why it's not a violation (test fixtures, array indices, and the like).
5. **R5** — no vault/financial-value type may reach a `track(...)` analytics call. This boundary
   does not exist yet in the inherited code (no `track()` calls exist) — write the rule so it is
   ready to activate the moment one appears; do not invent a `track()` call to test it against.

## DEFINITION OF DONE — every item must be objectively checkable
1. `.eslintrc.boundaries.js` exists and `npx eslint --config .eslintrc.boundaries.js --no-eslintrc src --ext .ts,.tsx,.js` (or your working equivalent invocation — state the exact command you used in your report if it differs) runs to completion and exits with violations reported, not a config error.
2. `npx tsc --noEmit` exits 0 (unchanged from baseline).
3. `npx eslint src --ext .ts,.tsx,.js --max-warnings=0` (the EXISTING main gate, unmodified) exits 0 (unchanged from baseline).
4. `npx jest --ci` still reports 42 suites / 410 tests passing (unchanged from baseline).
5. Your boundary-lint run flags, at minimum, all of: `cardRoleEngine.getEffectiveFxCommission`, `useFxAbroad`/`fxAbroadEngine`'s `resolveFxAbroad`, the inline FX logic in `usePurchaseGate.ts`, the two hardcoded scores in `DecisionScreen.tsx`, the direct JSON imports in `useCardRatesDatabase.ts` and `useFxAbroad.ts`, and the numeric load-ladder literals in `installmentGate.ts`, `loanEngine.ts`, and `purchaseGateRules.ts`.
6. Your boundary-lint run does NOT flag anything in a file you can point to as genuinely architecturally clean (pick at least one such file in your report and confirm it is silent there).
7. `git diff --name-only` (from the worktree) shows only the two files named in SCOPE — nothing else changed.

## REQUIRED OUTPUT
Write `_orchestration/reports/E1-boundary-violations.md` containing:
- The full violation list your rules produced (file, line, rule, message) — this is the P2/P3
  cleanup backlog, so make it usable as one: group by rule, one line per violation.
- Every DoD command you ran and its verbatim output (or a representative excerpt if very long —
  say so if you trimmed it).
- Anything you could not do, and why.
- Any assumption you made that was not specified above (the R3 adapter-path judgment call
  especially — state plainly which path you used).
- Anything you noticed that seems wrong but was out of scope for this task.

## EXPLICITLY NOT IN SCOPE
- Fixing any violation you find. This is a diagnostic; the fix is a future task.
- Wiring `.eslintrc.boundaries.js` into `package.json` scripts, CI, or the main lint command.
- Adding, removing, or upgrading any dependency. If a rule genuinely requires a new ESLint plugin
  that isn't already installed, say so in your report instead of installing it.
- Reformatting, "cleaning up," or touching any file not named in SCOPE, even if you notice
  something that looks wrong while reading CONTEXT files.
- Restoring `newArchEnabled`, touching `app.json`, or anything related to OD-14 — unrelated to
  this task.
