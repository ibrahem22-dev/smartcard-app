// @deprecated NOT used at runtime (no importers). Duplicate re-export of
// ../i18n/locale (canonical). Kept for reference; safe to delete later.
export {
  getNormalizedLocale,
  getDeviceLanguage,
  isRTLLocale,
  resolveLanguage,
  readStoredLanguageChoice,
  persistLanguageChoice,
  type AppLanguage,
  type LanguageChoice,
  type LanguagePreference,
  type ResolvedLanguage,
} from '../i18n/locale';
