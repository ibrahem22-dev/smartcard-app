{
  "$comment": [
    "SCREENS THAT CANNOT RENDER WITHOUT AN INPUT THE HARNESS MUST NOT INVENT.",
    "",
    "The render harness gives every screen the context the app gives it — AuthProvider, a real",
    "navigator, an unlocked vault. It deliberately passes EMPTY route params, because filling them",
    "with plausible values would be inventing the screen's inputs, and a screen that renders only",
    "because the harness guessed its parameters has not been shown to render: it has been shown to",
    "render one guess.",
    "",
    "A screen listed here is REPORTED, never skipped and never hidden. Every entry must name the",
    "criterion or work package that will clear it, so the register cannot become somewhere to put a",
    "screen nobody wants to fix. `p2:gate -- render-harness` prints every entry on every run.",
    "",
    "AN ENTRY IS CLEARED by giving the screen a real fixture — a committed params file a reviewer",
    "can read — not by deleting the line."
  ],
  "requiresFixture": [
    {
      "screen": "src/screens/DecisionScreen.tsx",
      "needs": "route.params.verdict",
      "why": "DecisionScreen reads route.params.verdict, .fxComparison, .reason and .exchangeFeeWarning. A verdict is an ENGINE OUTPUT, and P2 renders facts and refusals rather than computing judgements (contract §1) — so the harness cannot manufacture one without inventing exactly the thing P2 is forbidden to derive.",
      "clearedBy": "D6 / WP-2.3",
      "clearedByNote": "WP-2.3 removes this screen's hardcoded 84/100 and 71/100 scores and its use of VERDICT_FALLBACK_REASONS as primary copy. The same work package establishes what a verdict actually looks like at the P2 boundary, and a committed fixture belongs with it.",
      "recordedAt": "2026-08-23",
      "recordedBy": "WP-1.4"
    }
  ]
}
