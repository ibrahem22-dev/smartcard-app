/**
 * Card DNA entry point for the canonical FX Compare sheet (X1).
 * This file re-exports the one component. A lookalike list here is the defect X1 names.
 * Card DNA content itself is P5 — this is only the route that opens the shared sheet.
 */
export { FxCompareSheet as FxCompareFromCardDna } from './FxCompareSheet';
export type { FxCompareSheetProps as FxCompareFromCardDnaProps } from './FxCompareSheet';
